# CS2 Price Bot

Telegram that shows prices for every skin in the game

## Installation

```bash
pip install -r requirements.txt
```

## Running

Go to `@BotFather`, create a bot.

Copy `.env.example`, rename it into `.env`, paste token into TOKEN variable.

Run:
```bash
python main.py 
```
